apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: app-secrets
  namespace: platform
spec:
  provider: azure
  parameters:
    keyvaultName: kv-platform-dev-001
    tenantId: <tenant-id>
