name: GitOps Validation
on:
  pull_request:
    paths:
      - "gitops/**"
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: echo "Validate GitOps manifests"
