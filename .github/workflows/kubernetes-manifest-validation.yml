name: Kubernetes Manifest Validation
on:
  pull_request:
    paths:
      - "kubernetes/**"
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Validate Kubernetes manifests"
