name: Rollback

on:
  workflow_dispatch:

jobs:
  rollback:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Rollback Information
        run: |
          echo "====================================="
          echo "Rollback Workflow"
          echo "====================================="
          echo "Automatic rollback is intentionally disabled."
          echo "This repository is a portfolio/reference implementation."
          echo "Rollback logic will be added when deployment is enabled."
